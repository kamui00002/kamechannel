/*
 * サイトの名前 — ここが唯一の出典。
 *
 * ■ 2026-09-14 に「KaMeChannel」から改名（運営者の判断）
 *   運営者が作っている学習アプリの相棒キャラ「ループちゃん」を案内役に立てたのに
 *   合わせて変えました。
 *   ⚠ ドメイン（astro.config.mjs の site）と Workers の名前（wrangler.jsonc の name）は
 *      変えていません。どちらも公開先そのもので、ドメインの判断（規約 §7）が済むまで触らない。
 *
 * ⚠ 名前を各ページに書き写さないこと。直すときに片方だけ古くなります
 *    （載せるものを変えたとき、トップの説明文だけ古いまま残ったのと同じ形）。
 */

/** 大見出しで折り返してよい位置で区切ったもの。つなげると名前になる。 */
export const SITE_NAME_PARTS = ['ループちゃんの', 'スラッシュコマンド', '教室'] as const;

export const SITE_NAME = SITE_NAME_PARTS.join('');
