# KaMeChannel — 運用規約

`https://kamechannel.com` の静的サイト。Astro + Cloudflare Workers。
**2026-09-01 に旧 WordPress（92記事）を捨てて作り直し中。まだ器だけ。**

このファイルはこのリポジトリ固有の規約です。ここに書いてあることは、
一般的な良し悪しより優先します。

---

## 1. いまどこにいるか

| | |
|---|---|
| 状態 | **器だけ。中身は未定** |
| 旧サイト | WordPress・92記事（2024-10-30〜2025-04-14）。**1年5ヶ月更新なし** |
| 旧記事 | **捨てる**（運営者の判断・2026-09-01）。URL の転送も張らない |
| バックアップ | `../kamechannel-backup/posts.json`（92記事の全文）と `urls.txt` |
| 旧サーバー | ConoHa（IP ＜旧サーバーの IP。data/cutover-notes.md 参照＞）。**メールは使っていないので解約可** |

**中身が決まるまで、意匠を作り込まないこと。** 何を載せるかで最適な誌面は変わります。

## 2. 中身の候補（運営者が検討中・2026-09-01）

- **(a) Claude のスラッシュコマンド解説** — 一次情報を持っている強み。ただし手で書く
- **(b) n8n で Discord に流れている内容を定期でブログ化** — 止まらない。ただし自動生成だけだと薄い

### 設計方針（おなけんブログの学習を持ち込む）

**自動で素材を集め、人が一言そえる。** おなけんブログが AdSense に
「有用性の低いコンテンツ」で 2 回落ちたあと、オリジナル文章の追加で価値が生まれた——
その構造をここでも使います。**集めただけのページを作らないこと。**

⚠ 旧サイトは**半年で 92 本書いて、そのあと 1 年 5 ヶ月止まりました**。
手で書き続ける前提の設計にすると同じことが起きます。**続く仕組みを先に決める。**

## 3. 土台（おなけんブログと同じ）

- Astro（静的出力）→ `dist/` → Cloudflare Workers Static Assets
- `trailingSlash: 'always'` / `build.format: 'directory'`
- ⚠ `wrangler.jsonc` の `routes` は**コメントのまま**。
  **DNS を Cloudflare に向けてから**有効にすること（向ける前に deploy すると失敗する）

## 4. まだ無いもの（作るときに決める）

- コンテンツの置き場と形式（YAML? Markdown? 自動生成?）
- 検証スクリプト（おなけんブログの `scripts/30-validate.mjs` にあたるもの）
- GitHub Actions（デプロイ、自動取り込み）
- アフィリエイト（A8 にサイト003として登録済み。貼るなら開示文を部品に持たせる）
- アクセス解析（おなけん側は PostHog を Cookie なしで運用）
