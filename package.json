{
  "name": "kamechannel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "harvest": "node scripts/10-harvest.mjs",
    "new:entry": "node scripts/20-new-entry.mjs",
    "validate": "node scripts/30-validate.mjs",
    "validate:dist": "node scripts/30-validate.mjs --dist",
    "validate:self-test": "node scripts/30-validate.mjs --dist --self-test",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:worker": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^7.2.2",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
